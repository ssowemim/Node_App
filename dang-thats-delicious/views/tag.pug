extends layout

block content
    .inner
        h2= #{tag}
        ul.tags
            each t in tags
                li.tag
                a.tag__link(href=`/tags/${t._id}` class=(t._id === tag ? 'tag___link--active' : ''))
                    span.tag__text = t.id
                    span.tag__count = t.count