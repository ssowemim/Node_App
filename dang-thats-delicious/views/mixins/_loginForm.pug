mixin loginForm()
    form.form(action="/login" method="POST")
        h2 login
        label(for="email") Emaill Address
        input(type="email" name="email")
        label(for"password")
        input(type="password" name="password")
        input.button(type="submit" value="Log In >")